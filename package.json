{
  "name": "yumzy",
  "version": "1.0.0",
  "description": "Yumzy - AI-Powered Food Delivery Platform",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm --prefix admin install",
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\" \"npm run start --prefix admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
